<div class="px-5 lg:px-12 py-10">
  <h1 class="text-2xl font-bold text-zinc-900 dark:text-zinc-100">Admin</h1>
  <p class="mt-2 text-zinc-600 dark:text-zinc-400">Signed in as <%= current_user.email %> (<%= current_user.role %>).</p>
</div>

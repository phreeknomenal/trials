<nav class="flex flex-row" aria-label="Breadcrumb">
  <ol role="list" class="flex items-center space-x-2 font-normal text-sm">
    <li>
      <div>
        <%= link_to root_path, class: "text-sky-600 hover:text-sky-800" do %>
          <%= render Utilities::IconComponent.new(:home) %>

          <span class="sr-only">Home</span>
        <% end %>
      </div>
    </li>

    <% crumbs.each do |crumb| %>
      <li>
        <div class="flex items-center space-x-2">
          <span class="text-ink-3 dark:text-ink-3-on-dark">/</span>

          <% if crumb[:path].present? %>
            <%= link_to crumb[:path], class: "text-ink-3 dark:text-ink-3-on-dark hover:text-sky-600" do %>
              <%= crumb[:name] %>
            <% end %>
          <% else %>
            <span class="text-ink-3 dark:text-ink-3-on-dark">
              <%= crumb[:name] %>
            </span>
          <% end %>
        </div>
      </li>
    <% end %>
  </ol>
</nav>

<div id="what-this-means-for-you"
  class="border border-line-2 dark:border-line-on-dark rounded-lg p-8"
  role="region"
  aria-labelledby="status-message-title"
>
  <%= render Typography::HeadingComponent.new(size: :h5, text: "What This Means For You") %>
    
  <% if status_message.present? %>
    <p 
      class="text-ink-2 dark:text-ink-2-on-dark text-base font-body leading-relaxed mb-4"
      aria-label="<%= aria_label %>"
    >
      <%= status_message %>
    </p>
  <% end %>

  <% if match_factors.any? %>
    <div class="mt-4">
      <h4 class="text-sm font-semibold text-ink dark:text-ink-on-dark mb-3">
        Your Match Factors
      </h4>
      <ul class="grid grid-cols-2 gap-4">
        <% match_factors.each do |factor| %>
          <li class="<%= factor[:css_classes] %>">
            <div class="flex-1">
              <p class="font-bold text-ink dark:text-ink-on-dark text-sm leading-none">
                <%= factor[:label] %>
              </p>
              <p class="text-ink-2 dark:text-ink-2-on-dark text-sm font-body mt-1 leading-none">
                <%= factor[:message] %>
              </p>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>


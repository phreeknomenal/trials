<% if @trials.present? && @trials.any? %>
  <section id="similar-trials" class="mb-12">
    <h2 class="text-2xl font-bold text-ink dark:text-ink-on-dark mb-6">More trials like this</h2>
    <p class="text-ink-3 dark:text-ink-3-on-dark mb-6">
      Other trials in the same area that might be a good match for you.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @trials.each do |trial| %>
        <div class="bg-surface dark:bg-surface-on-dark rounded-lg shadow-md border border-line dark:border-line-on-dark hover:shadow-lg transition-shadow p-6 flex flex-col">
          <div class="mb-4">
            <% if trial[:trial_score].present? %>
              <div class="flex items-center gap-3 mb-2">
                <div class="flex-shrink-0">
                  <%= render Utilities::ScoreProgressBarComponent.new(
                    score: trial[:trial_score],
                    label: "Match",
                    match_level: trial[:match_level]
                  ) %>
                </div>
                <span class="text-xs font-semibold px-2 py-1 rounded-full <%= match_score_badge_class(trial[:trial_score]) %>">
                  <%= case trial[:match_level]
                      when "excellent" then "Excellent"
                      when "good" then "Good"
                      when "fair" then "Fair"
                      else "Poor"
                      end %>
                </span>
              </div>
            <% end %>
          </div>

          <h3 class="text-lg font-semibold text-ink dark:text-ink-on-dark mb-2 line-clamp-2">
            <%= truncate(trial[:title] || trial[:nct_id], length: 60) %>
          </h3>

          <p class="text-xs text-ink-3 dark:text-ink-3-on-dark mb-3">
            NCT<%= trial[:nct_id] %>
          </p>

          <% if trial[:conditions].present? && trial[:conditions].any? %>
            <div class="mb-3 flex flex-wrap gap-1">
              <% trial[:conditions].take(2).each do |condition| %>
                <span class="inline-block text-xs px-2 py-1 rounded-full bg-sky-100 dark:bg-navy-900/30 text-sky-700 dark:text-sky-300">
                  <%= truncate(condition.is_a?(Hash) ? condition[:name] : condition, length: 20) %>
                </span>
              <% end %>
              <% if trial[:conditions].length > 2 %>
                <span class="inline-block text-xs px-2 py-1 text-ink-3 dark:text-ink-3-on-dark">
                  +<%= trial[:conditions].length - 2 %> more
                </span>
              <% end %>
            </div>
          <% end %>

          <% if trial[:locations].present? && trial[:locations].any? %>
            <div class="mb-3 flex items-center gap-1 text-xs text-ink-3 dark:text-ink-3-on-dark">
              <div class="mt-0.5 flex-shrink-0">
                <%= render Utilities::IconComponent.new("map_pin", size: 4) %>
              </div>
              <span><%= trial[:locations].first %></span>
            </div>
          <% end %>

          <div class="mb-4 flex items-center gap-1 text-xs text-ink-3 dark:text-ink-3-on-dark">
            <div class="w-2 h-2 rounded-full bg-good"></div>
            <span><%= trial[:status] || trial[:trial_status] || "Active" %></span>
          </div>

          <div class="mt-auto pt-4 border-t border-line dark:border-line-on-dark">
            <%= link_to "View Details", my_trial_path(trial[:nct_id]),
                class: "inline-block w-full text-center px-4 py-2 bg-navy-600 hover:bg-navy-700 text-white rounded-lg transition-colors font-medium text-sm" %>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<% end %>

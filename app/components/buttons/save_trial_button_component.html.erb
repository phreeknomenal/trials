<div data-controller="save-trial"
     data-save-trial-nct-id-value="<%= nct_id %>"
     data-save-trial-trial-title-value="<%= trial_title %>"
     data-save-trial-is-saved-value="<%= is_saved? %>"
     data-save-trial-match-score-value="<%= match_score %>"
     data-save-trial-trial-data-value="<%= trial_data_json %>"
     <% if is_saved? %>data-save-trial-saved-trial-id-value="<%= @saved_trial.id %>"<% end %>>
  <button type="button"
          data-save-trial-target="button"
          class="<%= button_classes %>"
          aria-pressed="<%= is_saved? %>"
          data-action="save-trial#toggleSave">
    <span data-save-trial-target="outlineIcon" class="<%= "hidden" if is_saved? %>">
      <%= render Utilities::IconComponent.new("bookmark", size: 5) %>
    </span>
    <span data-save-trial-target="solidIcon" class="<%= "hidden" unless is_saved? %>">
      <%= render Utilities::IconComponent.new("bookmark", size: 5, options: {fill: "currentColor", stroke: "none"}) %>
    </span>
    <span data-save-trial-target="label"><%= button_text %></span>
  </button>
</div>
